# ddo-info releases

Compiled release builds of the **ddopowerlevel** DungeonHelper plugin. The plugin
polls `version.json` on startup and self-updates from here. Source is private.

- `version.json` — latest version + download URL
- `ddopowerlevel-<version>.zip` — the packaged plugin (dll + deps.json)
