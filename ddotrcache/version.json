{
  "zip_url": "https://raw.githubusercontent.com/JamesCfer/ddo-info-releases/main/ddotrcache/ddotrcache-0.17.0.zip",
  "version": "0.17.0",
  "notes": "Empty the TR Cache: reads the cache via BankDataMessage and moves items to bank/backpack with every move server-confirmed. First published release. Also adds background loot collection: chest contents feed the shared community drop-rate database. Only one plugin per game client reports, so running several together never double-counts. Install the ddoloot plugin to browse the data."
}
