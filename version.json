{
  "zip_url": "https://raw.githubusercontent.com/JamesCfer/ddo-info-releases/main/ddopowerlevel-0.5.0.zip",
  "version": "0.5.0",
  "notes": "Fix the runaway CPU/lag (and the earlier popup freeze): the background state-writer used a fixed-period timer whose callback could overrun and re-enter, piling blocked SDK/network calls onto the thread pool. Writers are now non-reentrant and back off when the game is closed, so the plugin stays idle once you exit DDO."
}
